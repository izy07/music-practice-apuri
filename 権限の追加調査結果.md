# 権限の追加調査結果

## 🔍 ユーザーからのご指摘について

### 1. 📳 バイブレーション権限（VIBRATE）

**ご指摘**: 「ばいぶ入らないかも」

**調査結果**: ✅ **その通りです！**

#### 現状の実装
```typescript
// lib/notificationService.ts の実装
await Notifications.scheduleNotificationAsync({
  content: {
    title,
    body,
    sound: this.settings?.sound_notifications !== false,
    // vibrate の設定がない！
  },
  trigger: null,
});
```

#### 発見したこと
- ✅ `vibration_notifications`という設定項目はデータベースに存在
- ❌ しかし、通知送信時に**バイブレーションを制御するコードが存在しない**
- ❌ `expo-notifications`のAPIでバイブレーションを明示的に制御していない

#### 結論
**バイブレーション権限（VIBRATE）は現在のコードでは使用されていません。**

`expo-notifications`は以下のように動作します：
- Androidのシステム設定に従って自動的にバイブレーション
- アプリが明示的に制御する必要はない
- したがって、`VIBRATE`権限は不要の可能性が高い

---

### 2. 🚀 起動時実行（RECEIVE_BOOT_COMPLETED）

**ご指摘**: 「起動時実行って何？」

**調査結果**: ✅ **使用されていません！**

#### 「起動時実行」とは
Androidデバイスが起動（再起動）した時に、アプリが自動的に動作を開始できる権限です。

**一般的な用途**:
- アラームアプリ: 再起動後も目覚まし時計を復元
- カレンダーアプリ: 予定の通知スケジュールを復元
- メッセージアプリ: バックグラウンド同期を開始

#### このアプリでの実装状況
```bash
# 起動時実行に関連するコードを検索
$ grep -r "BOOT_COMPLETED\|BootReceiver\|restoreNotification"

# 結果: 何も見つからない
```

**発見したこと**:
- ❌ デバイス起動時に実行されるコードが**存在しない**
- ❌ 通知スケジュールを復元するコードが**実装されていない**
- ⚠️ `expo-notifications`が自動的に権限を追加している可能性

#### 結論
**起動時実行権限（RECEIVE_BOOT_COMPLETED）は現在のコードでは使用されていません。**

---

## 📊 修正が必要な権限

| 権限 | 当初の説明 | 実際の状況 | 対応 |
|------|-----------|-----------|------|
| VIBRATE | 「通知のバイブレーション制御」 | ❌ コードで使用されていない | 削除検討 |
| RECEIVE_BOOT_COMPLETED | 「通知スケジュールの復元」 | ❌ 実装されていない | 削除検討 |

---

## 🎯 推奨される対応

### オプション1: 権限を削除する（推奨）

現在使用していない機能のため、権限を明示的にブロックする：

```typescript
// app.config.ts
blockedPermissions: [
  // ... 既存の設定 ...
  'android.permission.VIBRATE',
  'android.permission.RECEIVE_BOOT_COMPLETED',
]
```

**メリット**:
- より最小限の権限で動作
- ユーザーの信頼性向上
- Google Play審査で有利

**デメリット**:
- システムのデフォルトバイブレーションも無効になる可能性
- 将来的に機能を実装する場合は権限を追加する必要がある

### オプション2: 将来の実装に備えて残す

今後の機能追加を見越して権限は残しておく：

**メリット**:
- 将来的にバイブレーション制御機能を追加しやすい
- 起動時の通知復元機能を追加できる

**デメリット**:
- 現時点では不要な権限を要求することになる

---

## 🔄 推奨される実装（もし機能を使うなら）

### バイブレーション機能を実装する場合

```typescript
// lib/notificationService.ts
await Notifications.scheduleNotificationAsync({
  content: {
    title,
    body,
    sound: this.settings?.sound_notifications !== false,
    // バイブレーションの設定を追加
    vibrate: this.settings?.vibration_notifications !== false ? [0, 250, 250, 250] : undefined,
  },
  trigger: null,
});
```

### 起動時実行を実装する場合

1. ブート時にアプリを起動するレシーバーを追加
2. 保存されている通知スケジュールを復元
3. `RECEIVE_BOOT_COMPLETED`権限を明示的に宣言

```typescript
// 実装例（現在は未実装）
if (Platform.OS === 'android') {
  // Androidのブート時にスケジュールを復元
  await Notifications.setNotificationHandler({
    handleNotification: async () => ({
      shouldShowAlert: true,
      shouldPlaySound: true,
      shouldSetBadge: false,
    }),
  });
}
```

---

## ✅ 最終推奨

### すぐに実施すべき修正

```typescript
// app.config.ts の android セクション
android: {
  permissions: ['RECORD_AUDIO', 'MODIFY_AUDIO_SETTINGS'],
  
  blockedPermissions: [
    'android.permission.READ_EXTERNAL_STORAGE',
    'android.permission.WRITE_EXTERNAL_STORAGE',
    'android.permission.READ_MEDIA_IMAGES',
    'android.permission.READ_MEDIA_VIDEO',
    'android.permission.READ_MEDIA_AUDIO',
    // 本番ビルドでは開発用権限も除外
    ...(isEasBuild || process.env.NODE_ENV === 'production' ? [
      'android.permission.SYSTEM_ALERT_WINDOW',
    ] : []),
    // 使用していない権限を明示的にブロック
    'android.permission.VIBRATE',
    'android.permission.RECEIVE_BOOT_COMPLETED',
  ],
}
```

### 更新後の権限リスト

| 権限 | 必要性 | 状態 |
|------|--------|------|
| 🎤 マイク | **必須** | ✅ 必要 |
| 📷 カメラ | オプション | ✅ 必要（使用時のみ） |
| 🔊 音声設定 | **必須** | ✅ 必要 |
| 🌐 インターネット | **必須** | ✅ 必要 |
| 📡 ネットワーク状態 | **必須** | ✅ 必要 |
| 💤 スリープ防止 | 推奨 | ✅ 必要 |
| 📱 他のアプリの上に表示 | 開発時のみ | ⚠️ 本番から除外済み |
| 📳 バイブレーション | - | ❌ **不要（削除推奨）** |
| 🚀 起動時実行 | - | ❌ **不要（削除推奨）** |
| 🛡️ Google Play | **必須** | ✅ 必要 |

---

## 📝 まとめ

ユーザーのご指摘は完全に正しく、以下の2つの権限は現在のコードでは使用されていません：

1. ✅ **バイブレーション (VIBRATE)** - 削除推奨
2. ✅ **起動時実行 (RECEIVE_BOOT_COMPLETED)** - 削除推奨

これらの権限を削除することで：
- 🎯 より最小限の権限で動作
- 🔒 ユーザーのプライバシー保護が強化
- ✅ Google Play審査での承認率が向上

**作成日**: 2026年9月26日  
**調査者**: Cloud Agent
