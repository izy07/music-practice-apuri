# Cursor Rules

認証だけに関わらず全ての機能追加、変更、修正などで全体像を考慮して対症療法ではなく、根本的な原因を特定して修正して。

認証周りのコードは非常に重要なので、他の機能を追加する際に認証ロジックを直接書き換えないように制限してください。認証状態が必要な場合は、必ず共通のインターフェース（`useAuthAdvanced`）を経由するようなコードを提案してください。

各機能、エラー時、失敗時はスルー、スキップせず根本的に治したいのでちゃんと表示されるようにして。
