/**
 * Platform-specific implementations live in:
 * - `BottomBannerAd.native.tsx` (iOS/Android)
 * - `BottomBannerAd.web.tsx` (Web)
 *
 * This fallback should never be used, but must stay web-safe.
 */
export const BottomBannerAd = () => null;

