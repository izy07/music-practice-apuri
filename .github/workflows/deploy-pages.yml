name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

# GitHub Pagesの自動デプロイを有効化

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: チェックアウト
        uses: actions/checkout@v4

      - name: Node.js セットアップ
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 依存関係のインストール
        run: npm ci --legacy-peer-deps

      - name: Webビルド（GitHub Pages用）
        run: |
          echo "🔧 ビルドを開始します..."
          npm run build:web:github || {
            echo "❌ ビルドが失敗しました"
            echo "📁 distディレクトリの内容:"
            ls -la dist/ || echo "distディレクトリが存在しません"
            exit 1
          }
          echo "✅ ビルドが完了しました"
        env:
          EXPO_PUBLIC_WEB_BASE: /music-practice-apuri
          GITHUB_PAGES_BASE: /music-practice-apuri
          EXPO_PUBLIC_SUPABASE_URL: ${{ secrets.EXPO_PUBLIC_SUPABASE_URL || 'https://uteeqkpsezbabdmritkn.supabase.co' }}
          EXPO_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.EXPO_PUBLIC_SUPABASE_ANON_KEY || '' }}
          NODE_ENV: production

      - name: ビルド成果物の確認
        run: |
          echo "📁 distディレクトリの内容:"
          ls -la dist/ || echo "❌ distディレクトリが存在しません"
          echo ""
          echo "📄 主要ファイルの確認:"
          [ -f dist/index.html ] && echo "✅ index.html が存在します" || echo "❌ index.html が見つかりません"
          [ -f dist/404.html ] && echo "✅ 404.html が存在します" || echo "⚠️  404.html が見つかりません"
          [ -f dist/.nojekyll ] && echo "✅ .nojekyll が存在します" || echo "⚠️  .nojekyll が見つかりません"
          [ -f dist/metadata.json ] && echo "✅ metadata.json が存在します" || echo "⚠️  metadata.json が見つかりません"

      - name: 404.htmlの確認と作成
        run: |
          if [ ! -f dist/404.html ]; then
            echo "⚠️  404.htmlが見つかりません。作成します..."
            if [ -f dist/index.html ]; then
              cp dist/index.html dist/404.html
              echo "✅ 404.htmlを作成しました"
            else
              echo "❌ index.htmlが見つからないため、404.htmlを作成できません"
              exit 1
            fi
          else
            echo "✅ 404.htmlが存在します"
          fi

      - name: .nojekyllファイルの作成
        run: |
          echo "" > dist/.nojekyll
          echo "✅ .nojekyllファイルを作成しました"

      - name: ビルド成果物のアップロード
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: GitHub Pagesへのデプロイ
        id: deployment
        uses: actions/deploy-pages@v4

